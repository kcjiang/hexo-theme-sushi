
<%
var bodyClassTemplate = "";
if (is_home()) bodyClassTemplate = "home-template paged-next";
if (is_post()) bodyClassTemplate = "post-template";
if (is_page()) bodyClassTemplate = "page-template";
if (is_tag()) bodyClassTemplate = "tag-template";
if (is_archive()) bodyClassTemplate = "archive-template";
if (is_category()) bodyClassTemplate = "category-template";
%>

<!DOCTYPE html>
<html lang="<%=config.language%>">

<%-partial('partials/head')%>

<body class="<%=bodyClassTemplate%>">
  <%-partial('partials/icons')%>

  <div class="site">
      <%-partial('partials/header')%>

      <% if (is_home()) { %>
          <%-partial('partials/cover')%>
          <%-partial('partials/featured')%>
      <% } %>

      <div class="site-content">
          <%- body %>
      </div>

      <%-partial('partials/footer')%>
      <%-partial('partials/modal')%>
      <%-partial('partials/pswp')%>
  </div>

  <script
      src="https://code.jquery.com/jquery-3.5.1.min.js"
      integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
      crossorigin="anonymous">
  </script>
  <% if (theme.scripts !== undefined && theme.scripts.length > 0) { %>
    <!-- scripts list from theme config.yml -->
    <% theme.scripts.forEach(url => { %>
      <script src="<%=url%>"></script>
    <% }); %>
  <% } %>

</body>
</html>
